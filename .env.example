# Used only by `npm run db:seed:local` / `db:seed:remote` to create the first
# login. Change the password after first login (no self-service change screen
# yet). Database access itself is via the D1 binding in wrangler.jsonc, not
# an env var.
SEED_OWNER_NAME="Farm Owner"
SEED_OWNER_USERNAME="owner"
SEED_OWNER_PASSWORD="changeme123"
